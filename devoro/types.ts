interface sidelinks {
    id: number,
    label: string
}

interface user {
    id?: string,
    username?: string,
    email?: string,
    password?: string
}

interface post {
    id?: string,
    text?: string,
    timestamp?: string,
}

